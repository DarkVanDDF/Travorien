/* eslint-disable @next/next/no-img-element -- curated public-source road-trip imagery is rendered with visible provenance. */
import ProductNav, { ProductBrand } from "./ProductNav.tsx";
import { signatureDrives } from "./data/product-content.ts";

export default function DriveCollectionPage() { return <main className="drive-collection"><ProductNav /><header><p className="v2-overline">Great Drives of China</p><h1>Choose the road<br />before the rental car.</h1><p>Five journeys designed around landscape, rhythm and who the road suits. Every route is content-ready; only Yunnan currently enters the transaction-ready demo.</p></header><section>{signatureDrives.map((drive, index) => <article key={drive.id}><div className="drive-collection-image"><img src={drive.heroMedia.imageUrl} alt={drive.heroMedia.alt} /><span>0{index + 1}</span><a href={drive.heroMedia.sourceUrl} target="_blank" rel="noreferrer">{drive.heroMedia.author} · {drive.heroMedia.licenseNote}</a></div><div><small>{drive.region} · {drive.recommendedDays} days</small><h2>{drive.name}</h2><p>{drive.summary}</p><div>{drive.themes.slice(0, 4).map((theme) => <span key={theme}>{theme}</span>)}</div><p className="drive-status">{drive.transactionStatus === "transaction-ready-demo" ? "Transaction-ready demo · Yunnan graph and mock inventory" : "Content-ready · no vehicle or hotel inventory connected"}</p><a href={`/road-trips/${drive.slug}`}>Explore the drive →</a></div></article>)}</section><footer className="product-footer"><ProductBrand /><p>Drive China Your Way.</p><a href="/plan">Ask the Travel Advisor</a></footer></main>; }
