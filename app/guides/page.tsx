import type { Metadata } from "next";
import GuideCollectionPage from "../GuideCollectionPage.tsx";
export const metadata: Metadata = { title: "China Road Trip Guides | Travorien", description: "Source-bound driving guidance and editorial inspiration for inbound China self-drive travelers." };
export default function GuidesPage() { return <GuideCollectionPage />; }
